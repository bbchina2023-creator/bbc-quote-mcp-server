# DEV-016.40 Staging Release Contract

Status: RC1 calculation/document layer verified. Primary manager workflow is direct ChatGPT intake from the current deal documents. MCP staging remains an optional integration contour and is not a release blocker for the manager workflow.

## Primary manager workflow — frozen

For every new deal:

1. The manager provides the input documents for that deal in the current ChatGPT conversation.
2. ChatGPT reads and reconciles only those current-deal documents as the source of deal-specific values.
3. If a downstream deterministic operation requires Drive IDs/URLs, ChatGPT itself stores/resolves the files in Google Drive and passes the required references. This is an internal technical bridge, not a manager step.
4. Previous deals may supply only approved model rules, mappings and regression expectations. Their client, route, payment schedule, amounts, items or other deal-specific values must never be copied into a new deal.
5. ChatGPT asks the manager only for information that is actually absent from all current-deal sources or is genuinely contradictory after source-priority reconciliation.
6. The deterministic calculation/document layer performs the financial calculations and produces the verified Google Sheets/XLSX/PDF package.
7. QA must reconcile source lineage, calculation totals, payment totals, unit economics and client-document rendering before release.

This direct-chat path is the primary production UX. Drive-first is permitted only as an internal technical bridge. The manager is not required to manually move files between ChatGPT, Drive and backend modules.

## Isolation of optional MCP staging

- Worker: `bbc-quote-mcp-server-staging`
- Branch: `dev-016.40-oauth`
- Entry point: `src/staging.ts`
- Production Worker remains a separate contour.
- Production generator remains the frozen `v4.1.37 / DEV-016.37` contour.
- MCP/Worker work must not delay or complicate the primary direct-chat manager workflow.

## Required deterministic backend candidate

- Required backend version after the latest source-only corrections: `4.1.43 / DEV-016.43`
- Source artifact: `BBC_KP_Generator_Code_v4.1.43_DEV-016.43_SOURCE_ONLY_QUESTION_HARDENING.gs`
- SHA-256: `26e4918959d1f8bee77203b31cc190c9b935304a6762b7ba145240d59c22fed6`
- Drive file ID: `1YqiqfNvI8zVvSplYH0Nz3bFE63J_ozI5`
- Base verified calculation/document artifact: `BBC_KP_Generator_Code_v4.1.40_DEV-016.40_BENCHMARK_ALIGNED.gs`
- Base Drive file ID: `14RxWS3Pe6KaOq8VnVek8K5wMh_429AUN`
- Master template ID: `1JOn1RJ013e6stGWBoqOD3XgnM6tMWizNQZyrLy3ClKw`
- Template title: `BBC — MASTER TEMPLATE КП DEV-016.40 BENCHMARK-ALIGNED`
- Template locale/time zone: `ru_RU` / `Asia/Shanghai`

### DEV-016.43 corrections

`4.1.43` continues the source-only manager-question hardening after `4.1.42`.

1. A unique source manager, deal scheme or payment schedule is accepted as deal data and cannot create a repeated confirmation question merely because an intermediate state was labelled `FOUND_UNCONFIRMED`.
2. A single explicit current-deal currency rate never becomes a technical manager question. Zero rates remain missing; multiple distinct rates remain a real conflict.
3. If the primary partner source provides an unambiguous international-freight USD amount and the deal has one applicable USD/RUB rate, a differing derived client-output RUB amount is treated as stale derived output when the primary source does not contain its own contradictory RUB amount. This is a technical finding, not a manager question.
4. If the current canonical item quantities sum to the same total as the authoritative deal total, but a derived client unit-economics sheet contains another total quantity, the derived sheet is treated as stale and produces a technical finding rather than a manager question.
5. A true contradiction in primary freight values, a true quantity conflict not resolved by the current canonical item set, or multiple source rates remains blocking and still produces a targeted question.

Pure contract regression: `runChatApiContractRegressionTest()` = PASS, 12 checks. Node syntax check = PASS.

## Backend identity gate — mandatory only before MCP staging activation

The only Apps Script project ID currently proven in project evidence is:

`1THgtoof8Lj_8jbmeu7R6PmNbB-zsn19VEMr94hkBxiIxOy0qzU3U_Jlc`

That project is associated with the frozen production generator `BBC КП Generator — MVP`.

Therefore MCP staging activation must first identify the exact Apps Script project/deployment referenced by the staging Worker secret `BBC_BACKEND_URL` and prove that updating it will not mutate the frozen production generator. No code/deployment update is allowed against the proven production project merely because staging returns backend version `4.1.38`.

The staging MCP proves only that `BBC_BACKEND_URL` currently reaches a backend returning `version: 4.1.38`. It does not expose the backend Script ID or deployment ID.

## RC1 reference transaction

- deal_id: `BBC-f35ad8a4-75d6-4003-9bef-cfca537cb922`
- external deal: `BBC-2026-002`
- verified total CNY: `94333.70786`
- verified total RUB: `1179171.34825`
- Google Sheet RC1: `1XUbDN7hZJKsDEcMrxSW8sZfAFCgJgtuktuTCIN8nlw8`
- XLSX Drive ID: `134M4x_KaSqbh6PLczQTiD-iESnsYCQl2`
- PDF Drive ID: `1n-b4VAi-kdC2QzKFnF3MpqQZi96aUauv`

## Acceptance gates for deterministic DEV-016.43

1. All deal-specific values come only from the current deal input documents or explicit answers to confirmed gaps/conflicts.
2. A unique non-conflicting source value must not generate a manager confirmation question.
3. A partial/stale canonical item row must not create item questions when the current-deal source documents already contain those item values.
4. A unique explicit current-deal source currency rate is selected without a manager question and without a write side effect in read-only package inspection.
5. Multiple distinct applicable source values remain real conflicts and must produce a targeted question.
6. A stale derived client freight value cannot override an unambiguous primary USD freight value plus the deal rate.
7. A stale derived unit-economics quantity cannot create a question when the current canonical items reconcile to the authoritative deal total.
8. Confirmed structured values suppress lower-authority source noise.
9. China domestic logistics is counted once and included in item invoice unit price.
10. Partner financial-services row maps to client `Финансовые услуги импортёра (если есть)` and is used only when applicable to the importer arrangement.
11. Partner agent-remuneration row maps to `Документооборот перевозчика`, is counted once, and is not duplicated in import totals.
12. Insurance is sourced from the VED partner where that is the approved rule.
13. Duty and import VAT are calculated by the internal quote formulas where that is the approved rule, even when they differ slightly from partner values.
14. Quote output contains exactly five workbook sheets and the client PDF uses only the client-facing sheets.
15. Static/grey benchmark labels remain populated; no blank benchmark labels are introduced by generation.
16. Payment schedule uses only the current deal's source documents and manager-confirmed missing data; it must never inherit payment splits from another deal.
17. Reference transaction totals reconcile to `94333.70786 CNY / 1179171.34825 RUB` across final total, payment total and unit aggregation.

## Current observed MCP staging mismatch

Fresh read-only E2E on 2026-08-17, request `REQ-113a1b37-9072-4f83-8b44-8ac4b33acb79`, for `BBC-f35ad8a4-75d6-4003-9bef-cfca537cb922` returns backend `version: 4.1.38`, `packageStatus: NEEDS_MANAGER_INPUT_BEFORE_CALCULATION`, and the pre-fix false manager/client/scheme/incoterms/destination/deliveryDays questions.

This confirms the optional staging runtime is stale. It does not block the primary direct-chat manager workflow. If MCP staging is resumed, the next action remains backend-identity proof followed by isolated `4.1.43` activation and E2E acceptance.
