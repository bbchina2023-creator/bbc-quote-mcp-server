# DEV-016.40 Staging Release Contract

Status: deterministic calculation/document layer and direct ChatGPT manager-flow are under release hardening. The primary manager workflow is direct ChatGPT intake from the current deal documents. MCP staging remains an optional integration contour and is not a release blocker for the manager workflow.

## Primary manager workflow — frozen

For every new deal:

1. The manager provides the input documents for that deal in the current ChatGPT conversation.
2. ChatGPT reads and reconciles only those current-deal documents as the source of deal-specific values.
3. If a downstream deterministic operation requires Drive IDs/URLs, ChatGPT handles that bridge internally.
4. Previous deals may supply only approved model rules, mappings and regression expectations. Their deal-specific values are not sources for a new deal.
5. The manager is asked only for information actually absent from all current-deal sources or genuinely contradictory after source-priority reconciliation.
6. Deterministic calculation/document logic performs the financial calculations and produces the verified Google Sheets/XLSX/PDF package.
7. QA reconciles source lineage, calculation totals, payment totals, unit economics and client rendering before release.

## Isolation of optional MCP staging

- Worker: `bbc-quote-mcp-server-staging`
- Branch: `dev-016.40-oauth`
- Entry point: `src/staging.ts`
- Production generator remains the frozen `v4.1.37 / DEV-016.37` contour.
- MCP/Worker work must not delay the direct-chat manager workflow.

## Required deterministic backend candidate

- Required backend candidate: `4.1.47 / DEV-016.47`
- Source artifact: `BBC_KP_Generator_Code_v4.1.47_DEV-016.47_PER_ITEM_PRODUCTION_RANGE_RULE.gs`
- SHA-256: `79eab7ebc552e022469c33e1e09d2a35b20da5907c5a1867db2a601bf4012e49`
- Drive file ID: `1ZgYQ7fxDeF66v0Zahjb9h-fZWOfCXmBx`
- Base verified calculation/document artifact: `BBC_KP_Generator_Code_v4.1.40_DEV-016.40_BENCHMARK_ALIGNED.gs`
- Base Drive file ID: `14RxWS3Pe6KaOq8VnVek8K5wMh_429AUN`
- Master template ID: `1JOn1RJ013e6stGWBoqOD3XgnM6tMWizNQZyrLy3ClKw`
- Template title: `BBC — MASTER TEMPLATE КП DEV-016.40 BENCHMARK-ALIGNED`
- Template locale/time zone: `ru_RU` / `Asia/Shanghai`

### DEV-016.47 — DEC-LOG-001 closed

`4.1.47` closes `DEC-LOG-001` with a deterministic presentation rule that creates no new deal fact.

When different production lead times are unambiguously attached to different item-level sources of the same current deal, the client-facing deal-level field is the outer minimum-maximum range of those item-level values. Example: `10` and `20` days becomes `10–20 дн.`; item ranges `8–12` and `15–20` become `8–20 дн.`.

The aggregation uses only current-deal item-level source evidence. Original item values and lineage are preserved. This case creates neither a manager question nor an owner decision. A real contradiction between applicable deal-level sources remains a targeted manager question.

Pure contract regression: `runChatApiContractRegressionTest()` = PASS, 24 checks. Node syntax check = PASS.

## Correct current reference transaction

The active reference transaction for this release contract is the latest source-backed organizer deal.

- deal_id: `BBC-15af9054-0cfd-414e-b0c9-90a50ac6d2a6`
- external quote: `BBC-2026-001`
- source file: `🟢出口表格 таблица экспорта 4.0.xlsx`
- source Drive ID: `1LwJS0-jc9XIwDva-GbcEaKbSKdMxcXLY`
- verified total CNY: `94333.70786`
- verified total RUB: `1179171.34825`
- final Google Sheet: `1CfrXEY26UacZvlmnIxm4wkiCMizTfzzutO7kxxaHnME`
- final XLSX: `1jQG8OniAsiIgIGxid2pu4ZDSVMlfkCFB`
- final PDF: `1u8not3oBikE3My70jhrsdkE_B9OVwCfY`

## Acceptance gates for deterministic DEV-016.47

1. Deal-specific values come only from current-deal documents or explicit answers to actual gaps/conflicts.
2. A unique non-conflicting source value creates zero manager questions.
3. Missing data creates one targeted question.
4. A real conflict between applicable sources creates one targeted question.
5. Lower-authority derived/client/template noise cannot override a unique primary source value.
6. A partial/stale canonical item row cannot create item questions when the current-deal source already contains the item values.
7. A unique current-deal currency rate is selected without a manager question or write side effect in read-only inspection.
8. Multiple distinct applicable currency rates remain a blocking conflict.
9. Explicit duty method/rate and import VAT rate from the current deal are source-confirmed without duplicate confirmation flags.
10. VAT refund does not require a duplicate supplier VAT-rate field when price ex VAT, price inc VAT and nominal VAT rate already reconcile; an explicit conflicting rate remains blocking.
11. A stale critical check no longer reproduced by the current deterministic model is a technical finding, not a manager question.
12. `CUS-001` exists only for a customs/VAT problem reproduced by the current deterministic model.
13. Approved BBC commission rules override stale lower-authority percentages; an explicit confirmed deal override remains an owner decision.
14. Confirmed canonical certification suppresses lower-authority certification noise.
15. Multi-item production lead times attached to separate item sources aggregate to one source-only min-max client range with zero manager questions and zero owner decisions.
16. Nested item ranges aggregate by the outer minimum and maximum bounds.
17. A true deal-level production-time conflict remains a targeted manager question.
18. China domestic logistics is counted once and included in item invoice unit price.
19. Partner financial-services row maps to client `Финансовые услуги импортёра (если есть)` only when applicable.
20. Partner agent-remuneration maps to `Документооборот перевозчика`, counted once.
21. Insurance follows the approved partner-source rule.
22. Duty and import VAT follow the approved internal quote formulas.
23. Quote output contains exactly five workbook sheets and client PDF uses only client-facing sheets.
24. Static/grey benchmark labels remain populated.
25. Payment schedule uses only current-deal evidence and confirmed missing data.
26. Reference totals reconcile to `94333.70786 CNY / 1179171.34825 RUB` across final total, payment total and unit aggregation.

## Backend identity gate — only before optional MCP staging activation

The only Apps Script project ID currently proven in project evidence is `1THgtoof8Lj_8jbmeu7R6PmNbB-zsn19VEMr94hkBxiIxOy0qzU3U_Jlc`, associated with the frozen production generator.

The optional staging contour still returns backend `version: 4.1.38`. Its exact Apps Script project/deployment identity must be proven before isolated staging activation of `4.1.47`.
